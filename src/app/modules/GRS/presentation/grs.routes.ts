import { Route } from "@angular/router";
import { ReservationsComponent } from "./reservations/reservations.component";

export const GRS_ROUTES: Route[] = [
    { path: 'reservation', component: ReservationsComponent }
]